import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
  outerContainer: {
    margin: '20px auto',
    maxWidth: 'fit-content',
    textAlign: 'center',
    color: 'white',
  },
  innerContainer: {
    margin: '20px auto',
  }
}));
